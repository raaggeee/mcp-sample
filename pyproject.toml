[project]
name = "mcp-sample"
version = "0.1.1"
description = "This is a sample mcp server. It explains how to implemement and execute a MCP server locally and remotely."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=3.3.1",
    "pysqlite3>=0.6.0",
]
